import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function Profile() {
  return (
    <View style={{
      flex: 1, 
      alignItems: 'center',
      justifyContent: 'center', 
    
  }}>
      <Text style={{color: '#65C825',fontSize:14,fontFamily: 'Nunito_600SemiBold',
}}>
          We are currently working on this screen
      </Text>
  </View>
  
  )
}

const styles = StyleSheet.create({})